# 1. Use Tauri for Desktop Framework

We decided to build the local calendar desktop app using Tauri. It provides a lightweight native container, excellent memory efficiency, and uses the system's native WebView2 on Windows. Tauri leverages Rust for backend system operations and standard HTML/CSS/JS for the frontend, which fits the user's local development tools.
