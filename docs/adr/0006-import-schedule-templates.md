# 6. Shift Imported Events to Selected Date (Template Mode)

We decided that importing a schedule from a JSON file will function as a "Template Import". Instead of placing events on their original calendar dates, the application will shift all imported events to align with the currently selected date. This allows users to save common daily routines or schedules to a file and apply them to any day of their choosing.
