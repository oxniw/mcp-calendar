# 4. OS Theme Integration

We decided to support a system-adaptive theme (Sync with OS). The application will automatically detect whether the operating system is in Dark Mode or Light Mode (using CSS media queries or Tauri APIs) and adjust the visual appearance dynamically. The UI will define CSS variables for colors, ensuring a seamless transition between light and dark modes.
